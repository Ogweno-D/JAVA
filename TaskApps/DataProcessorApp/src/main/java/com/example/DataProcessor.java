package com.example;

public interface  DataProcessor {
    /**
     * Processes the input data and returns the result.
     *
     * @param inputData The data to be processed.
     * @return The processed result.
     */
    String process(String inputData);



}